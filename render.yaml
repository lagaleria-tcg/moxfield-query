services:
  - type: web
    name: moxfield-query
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: HOST
        value: "0.0.0.0"
      - key: PLAYWRIGHT_BROWSERS_PATH
        value: /ms-playwright
